// GENERATED COPY. Do not edit here.
// Source: Claudesidian vault, zz_System/Scripts/changelog-shared/Changelog.tsx
// Propagate with: python3 zz_System/Scripts/changelog-sync.py

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
L              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
R              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
T              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
J              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
9              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
3              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
O              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
E              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
j              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
T              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
z              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
z              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
_              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
S              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
S              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
P              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
q              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
2              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
0              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
2              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
6              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
0              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
9              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
0              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
3              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
J              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
L              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
R              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
S              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
J              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
D              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
F              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
9              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
5              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
J              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
A              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
T              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
2              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
9              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
5              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
J              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
H              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
A              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
G              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
E              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
L              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
O              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
G              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
_              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
S              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
H              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
E              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
M              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
A              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
_              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
V              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
E              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
R              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
S              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
I              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
O              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
1              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
O              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
L              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
F              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
H              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
E              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
Y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
Y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
Y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
Y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
M              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
M              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
D              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
D              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
J              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
H              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
J              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
O              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
[              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
]              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
L              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
[              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
]              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
j              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
F              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
[              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
]              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
S              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
H              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
L              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
D              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
S              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
E              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
[              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
]              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
J              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
S              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
O              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
j              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
D              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
j              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
D              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
[              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
]              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
J              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
$              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
$              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
D              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
E              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
D              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
E              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
[              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
]              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
!              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
1              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
1              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
0              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
A              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
R              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
W              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
L              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
L              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
P              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
D              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
T              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
|              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
|              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
T              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
+              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
O              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
R              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
P              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
R              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
E              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
R              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
$              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
A              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
A              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
H              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
T              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
q              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
q              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
q              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
B              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
|              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
|              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
B              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
$              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
$              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
2              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
2              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
P              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
z              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
z              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
j              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
j              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
0              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
·              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
A              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
A              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
&              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
y              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
H              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
k              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
A              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
H              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
L              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
A              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
P              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
,              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
b              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
*              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
-              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
c              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
N              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
m              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
=              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
w              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
"              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
(              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
.              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
?              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
·              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
$              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
{              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
`              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
'              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
v              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
s              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
:              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
i              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
<              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
/              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
R              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
>              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
)              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
}              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
x              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
p              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
r              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
d              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
f              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
u              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
t              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
               {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
C              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
h              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
a              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
n              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
e              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
l              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
o              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
g              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
;              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}

              {e.hand ? (
                <aside className="log-note log-note-hand">
                  {/* The note is the handwriting. Cropped photo of the printed page, cleaned but not
                      redrawn (the wrinkle is the point); the transcript lives in alt text only. */}
                  <img src={e.hand.src} alt={e.hand.transcript} width={e.hand.width} height={e.hand.height} loading="lazy" />
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : e.note ? (
                <aside className="log-note">
                  {e.note}
                  <div className="who">{tag(e.date)}</div>
                </aside>
              ) : null}
